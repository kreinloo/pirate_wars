/*

	pw_gameserver.js

*/

GameServer = (function () {

	var activeGames = {};

	var createGame = function (player1, player2) {

		//var game = new Game (player1, player2);
		//activeGames.push(game);
		var gameID = generateGameId();

		return gameID;
	};

	var generateGameId = function () {

		var id = new String(Math.random()).slice(2);
		if (activeGames[id] === undefined) {
			return id;
		} else {
			return generateGameId();
		}

	};

	var handleGameInfo = function (data) {

		if (data.gid !== undefined && activeGames[data.gid] !== undefined) {
			//activeGames[data.gid].handleGameInfo(data);
		}

	};

	return {

		createGame : createGame,
		handleGameInfo : handleGameInfo

	};

});

module.exports = GameServer;
